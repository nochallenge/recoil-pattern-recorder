{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for recoil-pattern-recorder: core IPC + events only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
