{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for recoil-trainer: core IPC + events only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
